name: Auto-Merge Beads JSONL (Disabled)

# DISABLED: Beads state is stored in external DB (BEADS_DIR).
# This repository no longer tracks `.beads/**`, so there is nothing to auto-merge.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled notice
        run: |
          echo "Disabled: Beads JSONL is external via BEADS_DIR; repo does not track .beads/**."
