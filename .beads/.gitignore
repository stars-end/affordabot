# SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Local-only version marker
.local_version

# Legacy database files
db.sqlite
bd.db

# Keep JSONL exports and config (source of truth for git)
!*.jsonl
!metadata.json
!config.json

# Ignore Beads merge artifacts (base/left/right) that are not meant for git.
beads.*.jsonl
beads.*.meta.json
