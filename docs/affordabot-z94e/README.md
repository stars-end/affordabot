# affordabot-z94e — Docs PR tracking (llm-common version management skill)

Tracks Affordabot PR `#185` (“docs-llm-common-skill”).

## Known Issue
PR `#185` was created before master fixes (e.g., the MinIO `expires` test change), so CI currently fails until it’s rebased onto latest `master`.

## Requirements
- Rebase PR `#185` onto latest `master`.
- Add `Feature-Key: affordabot-z94e` trailer to the commit(s).
- Link PR `#185` to this issue.

