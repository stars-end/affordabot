# Scraped Lane Cycles — Agent A (`bd-3wefe.13`)

Feature-Key: `bd-3wefe.13`  
Worker Role: Agent A - Scraped Evidence Moat  
Cycle Budget Executed: 10 / 10 (A1-A10)

| Cycle | Hypothesis | Gate Target | Tweak | Validation Command | Measured Before/After (or Blocker Evidence) | Impact On Final Package Quality | Next Tweak |
|---|---|---|---|---|---|---|---|
| A1 | Runtime provider provenance must be explicit, not inferred from docs/narrative. | D2 | Added `provider_label` on all search clients in `web_search_factory.py`; bridge now records `provider_source`. | `cd backend && poetry run pytest tests/services/llm/test_web_search_factory.py tests/services/pipeline/test_bridge_runtime.py::test_runtime_bridge_persists_policy_evidence_package_refs` | Before: Cycle 25 audit noted hardcoded/ambiguous provider identity. After: `run_context.search_provider_runtime.provider_source == client_label` and provider labels asserted for searxng/tavily/exa. | Stronger provenance for scraped primary artifact and provider auditability. | Add fallback counters tied to candidate audit outcomes. |
| A2 | Scraped ranker/portal-skip behavior needs measurable counters. | D2 | Extended `source_quality_metrics` with `portal_skip_count`, `official_reader_error_count`, `fallback_materialization_count`. | `cd backend && poetry run pytest tests/services/pipeline/test_bridge_runtime.py::test_runtime_bridge_prefetch_skips_portal_url_and_fetches_artifact_candidate tests/services/pipeline/test_bridge_runtime.py::test_runtime_bridge_records_selected_quality_when_artifact_exists_but_official_page_selected` | Before: only aggregate quality/no explicit skip-fallback counts. After: counters persisted and asserted in runtime tests. | Better artifact-selection diagnostics; easier to gate on portal skip and fallback patterns. | Add package-level lineage graph for missing family evidence. |
| A3 | Policy lineage pass requires explicit found/missing source-family graph. | D1 | Added `policy_lineage` block in bridge with expected families, presence flags, related artifacts, negative evidence, completeness score. | `cd backend && poetry run pytest tests/services/pipeline/test_bridge_runtime.py::test_runtime_bridge_records_lineage_negative_evidence_and_reconciliation_policy` | Before: no package-level lineage completeness object in run context. After: `policy_lineage` emitted with `lineage_presence` and `negative_evidence`. | Moves scraped lane from single-artifact proof toward lineage-aware package quality. | Improve extraction cards for citation-level parameter details. |
| A4 | Primary artifact parameter extraction needs raw/normalized/unit/denominator/category metadata. | D4 | Upgraded `_extract_primary_fee_facts_from_analysis` to emit rich fact fields + locator + confidence + money-format anomaly fact. | `cd backend && poetry run pytest tests/services/pipeline/test_bridge_runtime.py::test_runtime_bridge_extracts_primary_fee_facts_from_analysis_chunks` | Before: minimal `{field,value,unit,excerpt}` facts. After: facts include `raw_value`, `normalized_value`, `denominator`, `category`, `source_locator`, `currency_sanity`, ambiguity entries. | Better citation audit trail from scraped primary artifact. | Propagate ambiguity/sanity into builder parameter cards. |
| A5 | Malformed or ambiguous values must fail-closed at card level. | D4 | Enhanced builder `_build_parameter_cards` to produce `ambiguous` state when value/sanity is invalid and include citation metadata in excerpt. | `cd backend && poetry run pytest tests/services/pipeline/test_policy_evidence_package_builder.py::test_builder_marks_ambiguous_parameter_when_citation_sanity_fails` | Before: malformed values could be dropped or treated as resolved implicitly. After: malformed `$18.706.00` path produces `state=ambiguous` with explicit reason. | Reduces silent bad-parameter propagation into economic handoff. | Add explicit cross-source reconciliation records. |
| A6 | Secondary and primary facts need machine-readable reconciliation records. | D5 | Added `source_reconciliation.records` with statuses (`confirmed`, `source_of_truth_selected`) and per-field decisions. | `cd backend && poetry run pytest tests/services/pipeline/test_bridge_runtime.py::test_runtime_bridge_records_lineage_negative_evidence_and_reconciliation_policy` | Before: no packaged reconciliation ledger for overlapping values. After: reconciliation records emitted and asserted. | Prevents hidden merges/overwrites across source families. | Enforce anti-override policy flag for secondary sources. |
| A7 | Secondary search must never silently override primary artifact values. | D5 | Added reconciliation policy output: `source_of_truth_policy` + `secondary_override_blocked` boolean. | `cd backend && poetry run pytest tests/services/pipeline/test_bridge_runtime.py::test_runtime_bridge_records_lineage_negative_evidence_and_reconciliation_policy` | Before: override policy implicit and non-auditable. After: explicit policy string + guard boolean persisted in run context. | Hardens source-of-truth governance for Tavily/secondary rescue paths. | Add robustness coverage for source-shape drift. |
| A8 | Source-shape drift must be detected and labeled in-package. | D8 | Added `_detect_source_shape_drift`; metrics now persisted under `source_quality_metrics.source_shape_drift`; drift adds fail-closed reason. | `cd backend && poetry run pytest tests/services/pipeline/test_bridge_runtime.py::test_runtime_bridge_detects_source_shape_drift tests/services/pipeline/test_bridge_runtime.py::test_runtime_bridge_persists_policy_evidence_package_refs` | Before: no explicit drift signal for missing URL/snippet shape changes. After: drift object with counts and `drift_detected` flag is emitted and tested. | Improves fallback/source-drift robustness and regression visibility. | Add standalone scraped-lane verifier for D1/D2/D4/D5/D8. |
| A9 | Scraped-lane gate checks need a dedicated regression verifier. | D8 | Added `backend/scripts/verification/verify_scraped_lane_data_moat.py` + unit tests. | `cd backend && poetry run pytest tests/verification/test_verify_scraped_lane_data_moat.py` | Before: no focused verifier for scraped-lane D gates. After: script validates lineage, quality counters, extraction citation fields, reconciliation policy, drift signal. | Creates reusable guardrail for future cycle reruns. | Publish cycle ledger artifacts with measured deltas and commands. |
| A10 | The 10-cycle run must be handoff-ready and auditable. | D1,D2,D4,D5,D8 | Added cycle artifacts (`.md` + `.json`) documenting A1-A10 hypotheses, tweaks, validation, measured deltas, impacts. | `cd backend && poetry run pytest tests/services/llm/test_web_search_factory.py tests/services/pipeline/test_policy_evidence_package_builder.py tests/services/pipeline/test_bridge_runtime.py tests/verification/test_verify_scraped_lane_data_moat.py` | Before: Agent A cycles 8-10 were placeholders/not executed in existing docs. After: 10 substantive cycles documented and validated in this worker lane; targeted suite `54 passed`. | Produces auditable scraped-lane progress toward `decision_grade_data_moat`. | Run live Railway/Windmill probe to verify new fields on current package row. |

