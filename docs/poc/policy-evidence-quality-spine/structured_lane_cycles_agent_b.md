# Structured Lane Cycles — Agent B (`bd-3wefe.13`)

Worker role: Agent B - Structured Source Moat  
Scope: D0/D1/D3/D5/D8 improvements for structured-lane contribution quality.

| Cycle | Source Family Targeted | Gate Targeted | Code/Config/Data Tweak | Validation Command | Before/After or Blocker Evidence | Impact on Final Package Quality | Next Tweak |
|---|---|---|---|---|---|---|---|
| B1 | source_catalog_universe | D0 | Expanded structured catalog to 7 families; added `lane_classification` + `live_proven`. | `poetry run pytest tests/services/pipeline/test_structured_source_enrichment.py` | before: 2 rows; after: 7 rows (`true_structured=6`, `secondary_search_derived=1`). | Improves source-universe breadth and lane classification honesty. | Add unavailable and key-required classification coverage. |
| B2 | legistar_web_api + san_jose_open_data_ckan | D0, D8 | Catalog annotation now sets `live_proven=true` only for observed runtime candidates and never for secondary-search lane. | `poetry run pytest tests/services/pipeline/test_structured_source_enrichment.py::test_structured_source_enricher_returns_integrated_status_when_candidates_exist` | before: no strict live_proven semantics; after: integrated families marked live, secondary remains non-live. | Prevents false integration claims from unavailable/secondary sources. | Add policy-lineage keys on structured candidates. |
| B3 | legistar matter/event metadata | D1 | Added `lineage_metadata` fields (`matter_id`, `event_date`, `event_body_id`, `source_identity`). | `poetry run pytest tests/services/pipeline/test_structured_source_enrichment.py::test_legistar_matter_metadata_includes_provenance_and_non_id_facts` | before: lineage block absent; after: lineage metadata emitted for Legistar candidates. | Structured rows can be tied to policy lineage graph attributes. | Carry lineage into package-level structured provenance. |
| B4 | ckan + legistar structured candidates | D1, D5 | Added `policy_match_key`, `policy_match_confidence`, `reconciliation_status` metadata at enrichment stage. | `poetry run pytest tests/services/pipeline/test_structured_source_enrichment.py` | before: no reconciliation semantics; after: all enriched structured outputs carry match/reconciliation metadata. | Enables explicit conflict/source-of-truth reasoning for structured facts. | Enforce reconciliation fail-closed behavior in builder. |
| B5 | tavily_secondary_search | D3 | Builder defaults `structured_secondary_source` to `true_structured=false`; schema now preserves this field. | `poetry run pytest tests/services/pipeline/test_policy_evidence_package_builder.py::test_builder_keeps_structured_secondary_search_out_of_scraped_provenance` | before: secondary snippets could inflate structured depth; after: secondary lane cannot claim true-structured credit. | Removes D3 false positives driven by snippet-derived evidence. | Require true-structured contribution depth for structured pass. |
| B6 | legistar latest-event fallback | D5 | Added reconciliation guard in builder; unreconciled structured rows trigger `blocking_gate_present`. | `poetry run pytest tests/services/pipeline/test_policy_evidence_package_builder.py::test_builder_blocks_unreconciled_structured_latest_event_fallback` | before: unrelated latest-event rows could flow; after: fail-closed when no policy-key intersection. | Prevents wrong-policy structured rows from contaminating package truth. | Persist reconciliation status/confidence in structured provenance output. |
| B7 | structured provenance in package schema | D1, D3, D5 | Extended `StructuredSourceProvenance` with true-structured, match confidence, reconciliation, and lineage ids. | `poetry run pytest tests/services/pipeline/test_policy_evidence_package_builder.py` | before: provenance fields were minimal; after: machine-auditable lineage and reconciliation metadata included. | Improves policy lineage completeness and structured-source auditability. | Add robustness regression for source-shape drift. |
| B8 | structured shape drift | D8 | Added regression test: malformed structured payload (`dict` instead of `list`) must fail closed, not crash/pass. | `poetry run pytest tests/services/pipeline/test_policy_evidence_package_builder.py::test_builder_fails_closed_when_structured_shape_drifts` | before: no explicit drift regression evidence; after: deterministic fail-closed coverage. | Strengthens robustness against upstream schema drift. | Publish updated structured source catalog artifact for this lane. |
| B9 | structured source catalog artifact | D0 | Added `artifacts/structured_source_catalog_agent_b.json` with universe/access/lane/live fields. | `git diff -- docs/poc/policy-evidence-quality-spine/artifacts/structured_source_catalog_agent_b.json` | before: no Agent-B scoped quality-spine catalog artifact; after: versioned artifact exists. | Adds audit-ready catalog proof for gate review and handoff. | Publish B1-B10 ledger artifact for reviewer replay. |
| B10 | structured lane cycle ledger | D0, D1, D3, D5, D8 | Added JSON + markdown cycle ledger with before/after evidence and validation commands. | `git diff -- docs/poc/policy-evidence-quality-spine/structured_lane_cycles_agent_b.md docs/poc/policy-evidence-quality-spine/artifacts/structured_lane_cycles_agent_b.json` | before: no Agent-B cycle ledger; after: complete B1-B10 chain with measurable gate movement. | Makes structured-lane gate progress auditable and resume-friendly. | Run live-cycle runtime proof after dependency tasks land. |

## Aggregate validation run

```bash
cd backend
poetry run pytest tests/services/pipeline/test_structured_source_enrichment.py tests/services/pipeline/test_policy_evidence_package_builder.py tests/services/pipeline/test_policy_evidence_quality_spine_eval_cycles.py
```
