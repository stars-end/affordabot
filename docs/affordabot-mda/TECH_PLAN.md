# affordabot-mda — Secure secret management for Railway credentials

## Goal
Minimize manual secret handling while keeping CI and verification reliable.

## Open Questions (Needs User Input)
- Where should long-lived tokens live (GitHub secrets, 1Password, etc.)?
- Do we want separate tokens per environment (dev/pr/prod)?

