# bd-jxclm.14.4 Integration Evidence Report

## Scope

This pass combines:

- Windmill orchestration assets from PR #420 (`19c19da67af42d5ce663282a579b57c5c8b2da82`)
- Backend persisted-pipeline adapter/endpoints from PR #421 (`cc2f50661da365769f83e5486cd7f0a54f736b04`)

Worktree: `/tmp/agents/bd-jxclm.14.4/affordabot`  
Branch: `feature-bd-jxclm.14.4`

Integration-only changes were applied where required for compatibility:

1. Windmill trigger now forwards flow/job linkage in headers:
   - `X-PR-WINDMILL-FLOW-RUN-ID`
   - `X-PR-WINDMILL-JOB-ID`
2. POC flow `start_run` now sends required payload fields for backend adapter:
   - `run_label`, `query`, `family` (+ optional flags)

### Repair Pass (Finalize Propagation)

Follow-up repair scope for PR #422:

1. `finalize-report` now propagates failed/blocked downstream states from `read_response` and `analyze_response` instead of always returning `status: succeeded`.
2. Added endpoint tests for:
   - read failed -> finalize failed
   - analyze failed -> finalize failed
   - happy path still succeeds
3. Preserved child decision lineage in finalize `evidence` and emitted explicit finalize alert when downstream steps are non-succeeded.

## Commands Run

1. Worktree + fetch:
   - `dx-worktree create bd-jxclm.14.4 affordabot`
   - `git fetch origin master pull/420/head:pr-420 pull/421/head:pr-421`
2. Combined branch assembly:
   - merged `pr-420` into feature branch
   - merged `pr-421` into feature branch
3. Backend env setup:
   - `cd backend && poetry install`
4. Combined test matrix:
   - `poetry run pytest tests/services/test_persisted_pipeline.py tests/test_persisted_pipeline_poc_endpoints.py tests/ops/test_windmill_persisted_pipeline_contract.py tests/ops/test_windmill_contract.py -q`
5. Verification script:
   - `python3 scripts/verification/poc_persisted_pipeline_searxng_zai.py --reset --json`
6. Static checks on updated integration glue:
   - `poetry run pytest tests/ops/test_windmill_persisted_pipeline_contract.py -q`
   - `poetry run ruff check tests/ops/test_windmill_persisted_pipeline_contract.py ../ops/windmill/f/affordabot/trigger_pipeline_step.py`
7. Repair validation (finalize propagation):
   - `poetry run pytest tests/test_persisted_pipeline_poc_endpoints.py -q`
   - `poetry run pytest tests/services/test_persisted_pipeline.py tests/test_persisted_pipeline_poc_endpoints.py tests/ops/test_windmill_persisted_pipeline_contract.py tests/ops/test_windmill_contract.py -q`
   - `python3 scripts/verification/poc_persisted_pipeline_searxng_zai.py --reset --json`
   - `poetry run ruff check main.py tests/test_persisted_pipeline_poc_endpoints.py`
   - `poetry run black --check main.py tests/test_persisted_pipeline_poc_endpoints.py`
   - `git diff --check`

## Test Results

- Combined pytest matrix: `63 passed` (warnings only; FastAPI `on_event` deprecation)
- Windmill POC contract tests: `6 passed`
- Ruff on touched integration files: `pass`
- POC verification script: `PASS`
  - Includes checks for: stale fallback, fail-closed, zero-results behavior, idempotent replay, and Windmill linkage propagation.
- Finalize propagation endpoint tests: `8 passed`
  - finalize now fails when read/analyze failed
  - finalize still succeeds for happy path

## Contract Alignment Review

### 1) Endpoint paths

Aligned:

- Trigger script routes:
  - `/internal/pipeline/poc/start-run`
  - `/internal/pipeline/poc/search-materialize`
  - `/internal/pipeline/poc/read-extract`
  - `/internal/pipeline/poc/analyze`
  - `/internal/pipeline/poc/finalize-report`
  - `/internal/pipeline/poc/zai-search-canary`
- Backend exposes corresponding routes in `backend/main.py`.

### 2) Payload shape

Aligned after integration glue:

- `start_run` now sends required payload (`run_label`, `query`, `family`) from flow.
- Step payloads include `run_id` where required.
- Optional POC flags are now passed from flow input with defaults (`live`, `prefer_cached_search`, `allow_stale_fallback`, `skip_analysis`).

### 3) Trace/linkage propagation

Aligned after integration glue:

- Trigger forwards flow/job IDs in headers.
- Backend extracts linkage from headers and persists to `pipeline_runs`, then includes linkage in step responses.

### 4) Response envelope + decisions

Aligned:

- Branchable decision values observed in tests and verification script:
  - `fresh_snapshot`
  - `stale_backed`
  - `zero_results`
  - `provider_failed_no_fallback`

### 5) Status-code semantics

Aligned:

- Domain outcomes are returned as `200` (branch in Windmill by decision).
- Runtime failures return `503` from adapter endpoints.
- Trigger script raises on `>=400`, enabling native Windmill retry behavior.

## Live Execution Attempt (Local Backend + Trigger Script)

Attempted:

- `CRON_SECRET=test-secret poetry run uvicorn main:app --host 127.0.0.1 --port 8001`

Blocked:

- App startup fails due to mandatory DB connect on startup with no local Postgres:
  - `ValueError: DATABASE_URL is not set`

This prevents a full local HTTP loop (`trigger_pipeline_step.py` -> running backend server) in this machine context without provisioning Postgres/connection details.

## Windmill CLI Validation Attempt

Attempted static/sync validation only if available.

Blocked:

- `wmill` CLI is not installed/configured in this environment (`wmill: command not found`).

## Schedule Failure-Handler Note

This pass did not set schedule-level `on_failure` values because shape/target binding must be
validated against the live Windmill workspace/runtime contract. Current schedule YAML remains
unchanged (`on_failure: ""`) and requires live-instance validation before enabling schedules.

## Strategic Recommendation (HITL Lockdown)

Recommendation: `lock_mvp_architecture` at design level, with one operational gate still required.

Evidence supports locking:

1. Boundary is now coherent and test-backed:
   - Windmill owns orchestration/retry/branching.
   - Backend owns domain decisions/persistence.
2. Integration contract between #420 and #421 is aligned after minimal glue.
3. End-to-end behavior is verified in adapter-level tests + verification script.

Operational gate still required before calling it fully operationally complete:

- One live Windmill-run evidence pass against a reachable backend DB environment:
  - happy path
  - stale-backed path
  - fail-closed path
  - recorded flow/job IDs in persisted rows

## Remaining Blockers

1. No reachable Postgres in this local environment for running backend server startup.
2. No `wmill` CLI/workspace configuration in this local environment for flow sync/validation.
